############################################################
# Dashboard environment configuration                      #
# Copy this file to `.env.local` and fill in the secrets.  #
# `.env.local` is ignored by git (see .gitignore).         #
############################################################

# Supabase (server-side secrets - do not expose to client code)
SUPABASE_POSTGRES_PASSWORD=
SUPABASE_JWT_SECRET=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase client configuration (safe to expose to the browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ElevenLabs API key (used for analytics requests)
VITE_ELEVENLABS_API_KEY=
# Optionally override the calls endpoint if your workspace uses a different route
# VITE_ELEVENLABS_CALLS_ENDPOINT=/v1/convai/calls
# Optional: tune metrics fetch window/pagination
# VITE_ELEVENLABS_CONVERSATION_PAGES=5
# VITE_ELEVENLABS_METRICS_DAYS=30
# VITE_ELEVENLABS_CHART_POINTS=14
