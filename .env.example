############################################################
# Dashboard environment configuration                      #
# Copy this file to `.env.local` and fill in the secrets.  #
# `.env.local` is ignored by git (see .gitignore).         #
############################################################

# Supabase (server-side secrets - do not expose to client code)
SUPABASE_POSTGRES_PASSWORD=
SUPABASE_JWT_SECRET=
SUPABASE_SERVICE_ROLE_KEY=

# Supabase client configuration (safe to expose to the browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_LOGIN_DOMAIN=
VITE_SUPABASE_SERVICE_ROLE_KEY=
# Optional: override the confirmation redirect used in invitations
# VITE_SUPABASE_INVITE_REDIRECT=https://dashboard.egsai.dev/login

# ElevenLabs API key (used for analytics requests)
VITE_ELEVENLABS_API_KEY=
# Optionally override the calls endpoint if your workspace uses a different route
# VITE_ELEVENLABS_CALLS_ENDPOINT=/v1/convai/calls
# Optional: fetch metrics via an n8n webhook instead of hitting ElevenLabs directly
# VITE_N8N_WEBHOOK_URL=https://workflow.egsai.dev/webhook/elevenlabs/metrics
# Optional: tune metrics fetch window/pagination
# VITE_ELEVENLABS_CONVERSATION_PAGES=5
# VITE_ELEVENLABS_CHART_POINTS=14
# VITE_ELEVENLABS_CONVERSATION_PAGE_SIZE=100
# VITE_ELEVENLABS_AGENT_PAGES=5
# VITE_ELEVENLABS_AGENT_PAGE_SIZE=100
# VITE_ELEVENLABS_CONVERSATION_CACHE_TTL_MINUTES=15
# VITE_ELEVENLABS_METRICS_CACHE_TTL_MINUTES=15
# VITE_ELEVENLABS_AGENTS_CACHE_TTL_MINUTES=60
